export const colors = {
  // Values reference CSS variables from globals.css
  skyLight: "var(--rs-teal-light)",
  sky: "var(--rs-teal)",
  skyDark: "var(--rs-teal-dark)",
  blue: "#3b82f6", // user bubble blue
  slateText: "var(--rs-text)",
  border: "var(--rs-card-border)",
  cardBg: "var(--rs-card-bg)",
  modalTop: "var(--rs-card-bg)",
  modalBottom: "var(--rs-card-bg)",
};

export const radii = {
  sm: 8,
  md: 12,
  lg: 20,
  pill: 999,
};

export const shadow = {
  modal: "0 12px 40px rgba(2, 6, 23, 0.25)",
};

export const spacing = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
};


